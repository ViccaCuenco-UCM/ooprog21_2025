public class FixDebugOne {
    public static void main(String[] args) {
        System.out.println("Hello World!");
       
    /*
     List down the spotted Bugs below:
     1. Invalid class name with space — it should be `FixDebugOne` instead of `FixDebug One` 
     2. Incorrect capitalization method name — main instead of Main
     3. Incorrect parameter declaration — changed `String args` to `String[] args`
     4. Typo in the word`System` — correct spelling instead of `Systm` to `System`
     5. Missing semicolon — added `;` after `System.out.println("Hello World!")`
     */
    }
}
